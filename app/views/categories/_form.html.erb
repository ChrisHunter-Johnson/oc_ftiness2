<%= form_with(model: category, local: true) do |form| %>
  <% if category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <div class="container">
  <form>
   <div class="form-group row">
    <label for="activity_name" class="col-lg-1 col-form-label">Name </label>
    <div class="col-lg-11">
     <%= form.text_field :cat_name, id: :cat_name %>
    </div>
   </div>
   <div class="form-group row">
    <label for="activity_name" class="col-lg-1 col-form-label">Description </label>
    <div class="col-lg-11">
     <%= form.text_field :cat_description, id: :cat_description %>
    </div>
   </div>
  </form>
  <div class="actions">
    <%= form.submit %>
  </div>
 </div>


<% end %>
